# Known Limitations

A candid, code-verified register of Nessie's current limitations, modeled on the
"Known Limitations" candor practice from block/buzz's `ARCHITECTURE.md` (see
[docs/reviews/2026-07-23-buzz-comparison.md](reviews/2026-07-23-buzz-comparison.md)
§3, "Docs candor").

**How to read this.** Every entry below was checked against the code at the
cited path on **2026-07-23** — this table is not a copy of stale planning-doc
claims. Where a source doc (`docs/implementation-phases.md`, mostly the
2026-04-08/09 "Phase 2 code-level prerequisites" list) claimed a defect that the
code has since fixed, the entry is recorded as **Resolved** with the evidence,
so the register stays trustworthy rather than alarmist. Line numbers are
approximate and drift with edits.

**Status taxonomy**

- **Confirmed in code** — the limitation is present and verified at the cited location.
- **Partially mitigated** — a real mitigation exists in code but does not fully close the gap.
- **Fix in progress** — an active branch/spec addresses it as of 2026-07-23 (see note); the entry is worded to stay accurate whether or not that work has merged when you read this.
- **Stale / superseded** — the original concern no longer applies because the architecture changed (e.g. Cloud Run → self-hosted Hetzner).
- **Resolved** — a previously documented limitation that current code fixes; kept here only to correct a still-open claim in a source doc.

> **Update, later on 2026-07-23:** both in-flight fixes have landed on `main`.
> Silent budget-cap truncation (L1) is Resolved — stops are now classified and
> user-visible. Realtime channel-privacy fan-out (L2) was re-verified as
> already mitigated and is now locked in by regression tests; its residual is
> mid-connection membership revocation.

---

## Active limitations

| ID | Limitation | Where (file:line ≈) | Status | Impact / notes |
|----|------------|---------------------|--------|----------------|
| L1 | **Silent budget-cap truncation.** A per-run cap (iterations / tool calls / wallclock / tokens / cost / repeated-calls) used to end the run with an empty `finalText` and only a `console.warn`. | `worker/src/run/execute/budget-stop.ts` (classification + notice); wired in `worker/src/run/execute/run-job.ts` | Resolved (2026-07-23) | Cap stops are now classified (`iteration_limit` … `repeated_tool_calls`), recorded as a `run.budget_exhausted` `TaskEvent`, and always user-visible: partial answers are delivered with an explicit stop notice, and no-answer stops post the notice as the terminal message and fail the run. 2026-08-05: pause/resume is now real — cap stops write a durable `RunCheckpoint` at ~90% headroom, follow-up runs auto-load it, `POST /api/runs/:id/continue` resumes one-tap, and non-interactive runs auto-continue (`docs/plans/2026-08-05-run-budgets-context-and-research-routing.md`). Only the run-detail UI badge remains open. |
| L2 | **Realtime WS/SSE channel-privacy fan-out.** Historically events were published at org scope and delivered on org match alone, bypassing channel privacy. | `api/src/realtime/hub.ts` (`shouldDeliverWsNotification`); `api/src/lib/request-helpers.ts:433` (`canAccessChannelRealtimeEvent` via `getVisibleChannel`, `filterAuthorizedScopes`); wired at `api/src/index.ts:215` | Partially mitigated | Re-verified 2026-07-23: subscribe-time authorization (`filterAuthorizedScopes`, membership-derived SSE scopes, thread access checks) and delivery-time re-checks are all in place and locked in by `api/test/realtime-subscription-authz.test.ts`. The remaining gap is **mid-connection membership revocation**: a user who loses channel access keeps receiving that channel's events until they reconnect (`api/src/services/realtime-events.ts:100` TODO). |
| L3 | **Run execution idempotency is enqueue-level only.** At-least-once queue retries can still re-run a claimed job's side effects. | `worker/src/queue.ts:23-49` (`INSERT ... ON CONFLICT (idempotency_key) DO NOTHING`) | Partially mitigated | Duplicate *enqueues* are now deduped via a unique `idempotency_key`, and `QueueJob.idempotencyKey` is populated by callers (e.g. `worker/src/run/memory-consolidation.ts`). There is still no "already completed" guard on retry of an in-flight job, so a retried attempt can re-emit output. `docs/implementation-phases.md` #8 ("PARTIAL — schema field exists, enforcement missing") remains directionally accurate. |
| L4 | **Single active membership in the tenant context.** A user in multiple orgs/teams is bound to their first membership for role/tenant resolution; project/team switching is limited. | `api/src/services/users.ts:28-29` (`organizationMembers[0]`) | Confirmed in code | `docs/implementation-phases.md` #5. Auth *does* now enumerate memberships (`api/src/services/auth.ts` `loadUserMemberships`) and login guards `NO_MEMBERSHIP`, but the mapped `TenantContext`/`/me` role still hard-selects index `[0]`. Full multi-membership switching is not implemented. |
| L5 | **Tenant hierarchy cross-consistency is not DB-enforced.** `Channel` references organization, project and team via independent FKs; nothing constrains that they agree with each other. | `api/prisma/schema.prisma` (`model Channel` — separate `organization`/`project`/`team` relations) | Partially mitigated | `docs/implementation-phases.md` #10. **2026-08-06:** the prerequisite gap is closed — 39 child tables that carried `organization_id` as a bare scalar, plus the three knowledge tables' required `project_id`, now have real cascading FKs (migration `20260806150000`, `docs/security-audit-2026-06.md`), so a dangling tenant id is no longer storable and an org delete cascades. What remains is the original narrower point: no **composite** invariant guarantees that a channel's team, project and organization agree with each other. Low risk for API-created rows (the services set all three coherently); the gap is at the schema/raw-write boundary. **2026-09-03:** the deeper cause is that `Team.projectId` inverts the intended hierarchy — a project should belong to a team, not the reverse — so a coherent triple has nothing to anchor on. See `docs/standards/team-model.md`. |
| L6 | **Whole context re-sent each iteration; no auto-compaction.** Every agentic turn re-sends system prompt + tool schemas + full conversation + accumulated tool results, and `maxTokens` counts cumulative usage across turns. | `worker/src/run/context-compaction.ts`, `worker/src/run/context-window.ts`; analysis in `docs/context-window-optimization-audit.md` §1–2 | Resolved (2026-08-05) | Real compaction now folds the elder transcript into a rolling work-state note (verbatim-URL sources, closed tool groups, cooldown + bounded attempts, compaction invocations counted in run totals) at ~80% of a per-model context window; silent trimming is emergency-fallback only. Default run budgets are the deployment backstop (`NESSIE_RUN_BACKSTOP_*`), so long-horizon work no longer dies at a 50k default cap. |
| L7 | **Pub/Sub queue provider is not implemented.** Setting `config.queue.provider = 'pubsub'` warns and silently falls back to the Postgres queue. | `worker/src/index.ts:102-105` | Stale / superseded | No longer a real defect: production is self-hosted on Hetzner with a **Postgres-backed queue by design** (no Redis, no Pub/Sub) — the Cloud Run assumption behind `docs/implementation-phases.md` #12 is retired. Kept here only to flag that the config switch is a dead branch that could be removed. |
| L8 | **No proactive budget alerting.** `Budget.warnThresholdPercent` was a passive display level only — nothing notified anyone when a scope crossed 80% or was blocked, so overspend was only discovered by opening a dashboard. | `packages/runtime/src/budget.ts` (`evaluateBudget`); `worker/src/run/execute/budget-alert.ts`; `worker/src/control/budget-alert-dispatch.ts` | Resolved (2026-07-23) | The gate now fires **at most once per budget scope per period** on the first threshold crossing ('threshold') and on the first block ('blocked'): a durable `budget_alerts` marker dedupes, a `budget.threshold_alert` `TaskEvent` records it, and `budget.alert-dispatch` notifies org owners + scope managers via the shared push pipeline (respecting push preferences). Alerting never alters the gate verdict. **Still open:** rate-of-spend / anomaly detection and a run-detail UI badge (see `docs/plans/2026-06-14-enterprise-readiness-roadmap.md`, Nessie epic #40). |
| L10 | **The `PolicyRule` engine is the authorization vocabulary for 5 checks and bypassed by 123 `requireOwner` sites.** Seeded org-wide rules (`channel:view`, `admin:admin`, `agent:view/invoke/bind`, `tool:view`, knowledge defaults) are consulted by only 6 call sites; `admin:admin` is read by nothing, and `channel:view` is contradicted by the real channel gate. | `api/src/services/policy.ts:371-485` (`seedDefaultPolicies`); consumers: `api/src/routes/agents.ts:621,708`, `routes/triggers.ts:279`, `routes/knowledge-base-access.ts:122`, `routes/policy.ts:38`, `worker/src/run/pa-tools/provisioning.ts:371` | Confirmed in code | An operator can author a policy rule through the fully-built CRUD (`services/policy.ts:104-266`) that silently does nothing, because the family it targets (e.g. `admin:admin`) is gated by `requireOwner` instead. Fix means either routing owner-gated families through `checkPolicy` (large) or shrinking the seed to the four pairs actually consulted and documenting the narrower scope (small). See review 2026-09-05 §1 finding 1.8. |
| L11 | **Device push tokens are transferable across users and tenants by token value.** `POST /api/devices` reassigns an existing `DeviceToken` row to the caller's user/org on presenting its token value, fenced only by a monotonic `registrationVersion`; the sibling web-push surface explicitly refuses the equivalent transfer. | `api/src/routes/devices.ts:63-69,72,84` (transfer); `api/src/routes/web-push.ts:83,130-132` (refuses transfer); `DELETE /api/devices/:token` (`:120-127`) is correctly org+user scoped, so the module is internally inconsistent | Confirmed in code | A caller who learns another tenant's APNs/FCM token redirects that device's push notifications to their own org. Fix: require proof of possession before a cross-user transfer, or scope the `updateMany` to `{ token, userId }`. See review §1 finding 1.10. |
| L12 | **`NESSIE_AUTH_SECRET` is a single undifferentiated root key for ~10 cryptographic purposes, with no rotation path.** Session HMACs, executor-daemon challenges, the voice device HMAC, and every AES-256-GCM store (UOA refresh tokens, MCP OAuth tokens, mailbox/push credentials, webhook secrets) all derive from the same `sha256(secret)` with no purpose separation. `keyVersion` columns on three encrypted-credential tables are never read or written by any TypeScript in the repo. | `api/src/lib/server-context.ts:99-109` (resolution); `api/src/auth/session.ts:37-38`, `api/src/services/executor-daemon-auth.ts:11-12`, `api/src/services/voice/ledger-gemini-live.ts:137-140` (bare/ad-hoc HMACs); `packages/runtime/src/secret-crypto.ts:23-30` (`deriveSecretKey = sha256(secret)`, no HKDF, no purpose); `api/prisma/schema.prisma:1690,6055,7468` (unread `keyVersion` columns) | Confirmed in code | A leak of the one env var yields session forgery, executor-daemon impersonation, and decryption of every stored credential and UOA refresh token in one step. Worse, rotating the secret alone bricks every ciphertext — nothing in the code consults `keyVersion` to fall back to a previous key while a rotation drains. Fix needs a purpose-keyed HKDF derivation (`deriveSecretKey(secret, purpose, keyVersion)`) applied uniformly, plus persisting and honouring `keyVersion` on every encrypted row — a re-encryption migration, not a derivation swap alone. See review §3 finding 3.1. |
| L13 | **Three independent session-revocation authorities are checked on every request**, each documented as the one that supersedes the others. `AuthSession.revokedAt` is deliberately fail-open on absence pending a proof that never landed, so the live refresh-row check is what actually enforces logout today. | `api/src/lib/server-context.ts:208-213` (`tokenVersion`), `:215-228` (`AuthSession.revokedAt`, 30s cache, fail-open), `:230-243` (`hasActiveUserSession` live refresh-row lookup); `api/src/services/auth-session-registry.ts:3-13,45-53` (registry's own docstring names itself the sole authority) | Confirmed in code | Every authenticated request pays three DB round trips for one question ("is this session alive?"), and a future revocation feature has three places to get right. Fix is to finish the hand-off the registry docstring describes: prove issuance covers every path, flip `isAuthSessionRevoked` fail-closed behind a dated cutover, then delete the live-refresh-row check from `authenticateRequest`. See review §3 finding 3.2. |
| L14 | **Bootstrap owner-provisioning state is per-process, so multi-replica behaviour is undefined.** A bootstrap token minted on one replica is `401 TOKEN_INVALID` on any other; each replica logs its own bootstrap URL and the operator cannot tell which one a load balancer will pick. The token comparison itself was fixed to a timing-safe match. | `api/src/lib/server-context.ts:111-150` (`bootstrapTokenState` module closure); `api/src/auth/bootstrap.ts:10-13`; `api/src/routes/auth-core.ts:266-274` (per-replica validation) | Confirmed in code | Unrecoverable by design: a sticky-session miss during the 15-minute window means restarting the API, since expiry does not re-mint. Fix: persist the bootstrap token (hashed, with `expiresAt`) in a single-row table so every replica validates the same value, or document that bootstrap requires a single replica. See review §3 finding 3.9. |
| L15 | **Postgres advisory locking has no owning module.** ~46 call sites split across two mutually invisible lock namespaces (a 1-arg `hashtextextended` space with a `nessie:<domain>:<id>` key convention, and a 2-arg `hashtext`/`hashtext` space with no namespace prefix) and two `$queryRaw`/`$executeRaw` call styles; a lock taken under one namespace can never exclude one taken under the other. | Namespace A: `api/src/db/seed.ts:34`, `services/external-organization.ts:35`, `services/team-target.ts:112`, `services/user-session-lock.ts:21`, `services/refresh-token-family.ts:68`, `routes/team-provisioning.ts:127`. Namespace B: `services/resource-locks.ts:79-82`, `services/plans.ts:166-167`, `services/librarian.ts:152-153`, `services/personal-assistant.ts:60-61,127-128`, `services/external-agent.ts:61-62,121-122`, `services/deepsignal-digest.ts:191-192`. Already-correct-but-private helper: `api/src/services/team-principal.ts:210-219` | Confirmed in code | Advisory locks are the serialization primitive for org materialization, principal creation, session revocation, provisioning and run slots; a new call site has no surface to reuse and must guess between two namespaces. Choosing wrong produces no error, just no mutual exclusion. Fix: one owner (e.g. `packages/db/src/advisory-lock.ts`) exporting a typed key builder over the single namespace-A space; convert all ~46 sites in one deploy. See review §4 finding 4.6. |
| L16 | **No single service convention for reporting a domain failure.** Discriminated result unions (31 modules), thrown error classes (39 modules), three error classes that additionally carry an HTTP status, and two services that write the HTTP response themselves all coexist. | Result unions: `api/src/services/message-create.ts:50-88`, `run-continuation.ts:29-51`, `runs.ts:148,214`. Error classes: `team-principal.ts:15`, `uoa-session-context.ts:19`. Status-carrying: `global-agent-chat.ts:37-43`, `voice/voice-session.ts:34`, `voice/ledger-gemini-live.ts:37`. Reply-writing: `automatic-membership/access.ts:51`, `executor-fresh-verification.ts:39` | Confirmed in code | A caller cannot tell from a service's name whether a miss is a value or a throw, and the two reply-writing services cannot be called from the worker or an MCP tool. Fix: pick one convention (result unions for expected domain outcomes, thrown errors only for genuine faults), state it in `docs/architecture.md`, and start with the three status-carrying classes and the two reply-writing modules. See review §4 finding 4.10. |
| L17 | **Two contradictory retention policies for compliance artifacts serving the same stated purpose.** `token_ledger_events` deliberately has no `Organization` FK so ledger rows survive org deletion (billing/audit retention); `AuditLog` and `BudgetAlert` both cascade-delete with the org — the opposite policy for the same stated purpose. No org-deletion code path exists yet, so this is latent. | `api/prisma/schema.prisma:4715-4717` (ledger, no FK, comment states the reason); `:4703` (`AuditLog … onDelete: Cascade`, `packages/db/src/audit-chain.ts:1-18` — a per-org tamper-evident hash chain deleted with the org); `:4854` (`BudgetAlert … onDelete: Cascade`, added by migration `20260806150000`) | Confirmed in code | The comment on `BudgetAlert` describing it as a bare scalar "matching the token_ledger_events pattern" was fixed to match the schema; the underlying retention decision (should audit logs and budget alerts survive an org delete the way ledger rows do, or should ledger rows cascade too) is still open and belongs in `docs/standards/` (customer-billing.md is the closest home). See review §7 finding 7.6. |
| L18 | **`resource_locks` is an orphan subsystem: three REST endpoints, zero internal callers.** A correct advisory-locking implementation exists and is reachable over HTTP, but nothing in the worker, executor, or any file-write path acquires a lock before writing — the concurrency `docs/the-agents.md` used to promise for concurrent agent file writes does not exist. | `api/src/services/resource-locks.ts:59-142`; `api/src/routes/resource-locks.ts`; `api/src/register-api-routes.ts:68,116`; no other call site in the repo | Partially mitigated | `releaseResourceLock`'s weak scoping (any org member could release any agent's lock; a re-release could silently overwrite the release time) has been fixed to require the owning `agentId` and filter `releasedAt: null`; `docs/the-agents.md` no longer claims the acquisition happens. Whether to wire acquisition into the executor's file-write path or delete the model/service/routes is a product decision, still open. See review §7 finding 7.7. |
| L19 | **`Team` carries no `organizationId`; tenancy is a `project: { organizationId }` join re-stated at ~29 call sites**, with two near-identical helpers doing the same three-line derivation. | `api/prisma/schema.prisma:1885-1915` (`Team.projectId`, no `organizationId`); re-check sites include `api/src/routes/scoped-settings.ts:38`, `routes/ledger.ts:214`, `routes/secrets.ts:113`, `services/secret-vault-write.ts:58`, `services/team-target.ts:234`, `packages/mcp-manage/src/mcp-instances.ts:275`, `packages/team-admin/src/mailbox-connections.ts:160`; duplicate helpers: `packages/team-admin/src/channel-slugs.ts:56-79` (`loadChannelTeamProject`) and `packages/team-admin/src/channel-records.ts:76-100` (`loadTeamProjectScope`) | Confirmed in code | This is the same root cause as the `Team.projectId` FK inversion (see `docs/standards/team-model.md`): a team's organisation has two independent paths (`Team.externalOrgId` vs `team.project.organizationId`) agreed in exactly one place. The FK inversion that would give `Team` its own `organizationId` is already scoped in [docs/plans/2026-09-02-uoa-as-a-service-unification.md](plans/2026-09-02-uoa-as-a-service-unification.md). See review §7 finding 7.2. |
| L20 | **287 hand-cast `request.params as {...}` sites, unlike request bodies which are validated with `parseInput` + zod in the large majority of cases.** Bodies: 353 `parseInput` call sites vs. 14 raw `request.body as {...}` casts (those 14 are now fixed). Params: only 68 `parseInput(..., 'params')` sites vs. 287 raw casts in 73 files. No `setErrorHandler` exists to translate a malformed path segment (e.g. non-UUID id reaching a `@db.Uuid` column) into a clean 400. | e.g. `api/src/routes/projects.ts:53,78,155,245,289` (`const { projectId } = request.params as { projectId: string }`) | Confirmed in code | A raw `as {...}` param cast is the same "hand-written DTO" pattern already flagged and fixed for bodies, just on the request side; a malformed non-UUID id likely surfaces as an unhandled 500 rather than a validated 400. Fix (mechanical, ~70 files): adopt `parseInput(..., 'params')` with a per-route `z.object({ id: z.string().uuid() })` schema; add a `setErrorHandler` backstop for Prisma validation errors. See review §5 finding 5.1 and §6 finding 6.1 (error handler, fixed). |
| L21 | **96 of 149 route modules have no test importing them**, including security-relevant ones (`executors.ts`, `agent-mailbox.ts`, `app-connection-requests.ts`). Separately, 147 `as unknown as PrismaClient` test fakes exist; at least one models only 2 of the 10 Prisma delegates the service under test actually uses. | Coverage table: `api/test/` vs `api/src/routes/*.ts` (149 route files, 53 with an importing test); fake-completeness example: `api/test/execution-runner-scope.test.ts` (2 delegates) vs `api/src/services/execution-environments.ts` (10 delegates) | Confirmed in code | A missing Prisma delegate on a fake is `undefined` at runtime, which can surface as a TypeError disguised as a 401 rather than a clear test failure (see [docs/standards/testing.md](standards/testing.md) "Prisma fakes"). Fix: triage the 96 untested routes by security sensitivity and add route-level tests for auth/secrets/mcp/executor-control paths first; when extending a test's Prisma fake, teach it every delegate the exercised code path touches in the same commit. See review §8 findings 8.2/8.3. |
| L22 | **13 modules still exceed the 500-line cap after this pass.** Of the 18 files the review found over the AGENTS.md 500-line limit, the five services with the clearest seams (`inference-control-plane.ts`, `messages.ts`, `policy.ts`, `token-ledger.ts`, `agent-tool-policy-registry.ts`) were split; the remaining files are a follow-up. | `api/src/routes/agents.ts` (849), `routes/dashboards.ts` (674), `routes/knowledge-base.ts` (632), `routes/knowledge-base-files.ts` (607), `routes/browser-cloud.ts` (590), `routes/executors.ts` (571), `lib/request-helpers.ts` (563), `realtime/hub.ts` (533), `routes/auth-login.ts` (522), `routes/threads.ts` (509), `services/execution-environments.ts` (503), `services/agent-read-model.ts` (503), `services/uoa-avatar.ts` (502), `services/refresh-token.ts` (502), `routes/team-provisioning.ts` (502) — line counts as of 2026-09-05, re-verify before relying on them | Confirmed in code | Each is a real architectural signal per AGENTS.md § Code Quality — a split along cohesive responsibility seams, not a `-helpers`/`-extras` bucket. See review §4 finding 4.9. |
| L23 | **Failed/crashed-run token spend was dropped.** Completion and the budget-stop no-text path persisted invocation ledger events, but a run that threw mid-loop (crash/abort) lost its partial spend, making failed-turn cost impossible to attribute (buzz #1659). | `worker/src/run/execute/run-job.ts` (catch → `persistInvocationLedgerEvents`); accumulator threaded through `worker/src/run/agentic-loop.ts` | Resolved (2026-07-23) | The agent loop pushes every inference invocation into a caller-owned accumulator live, so the terminal failure path persists them (idempotent on `inferenceInvocationId`). Owners read spend split by run outcome at `GET /api/ledger/tokens/by-outcome`. |

## Resolved since documented (source doc still lists them open)

These correct still-open claims in `docs/implementation-phases.md`'s Phase 2
prerequisite list (statuses there were last touched 2026-04-09). Verified fixed
in current code.

| ID | Was documented as | Now | Evidence |
|----|-------------------|-----|----------|
| R1 | #3 "Agent binding has no authorization check" (NOT FIXED) | Resolved | `api/src/routes/agents.ts:259` gates `agent`/`bind` through `checkPolicy` and returns `403 POLICY_DENIED`; `bindAgentToChannel` is org-scoped (`api/src/services/agent-bindings.ts:19-45`). |
| R2 | #4 "`NESSIE_AUTH_SECRET` fallback to per-process random breaks multi-instance" (NOT FIXED) | Resolved | `api/src/lib/server-context.ts:162-172` requires the secret in `hosted`/`selfHosted` modes (FATAL `process.exit(1)` if unset); the ephemeral `randomUUID()` fallback only applies to single-instance `local` dev. |
| R3 | #7 "Worker ignores `actorContext`" (PARTIAL — carried but unused) | Resolved | `actorContext` is consumed for tenancy and policy in `worker/src/run/execute/budget-gate.ts:62-64`, `worker/src/run/execute/policy.ts:59-115`, `worker/src/run/execute/memory.ts:156-161`, and tool actor context in `worker/src/run/execute/agent-loop.ts:284`. |
| R4 | #9 "Agent model lacks ownership fields" (NOT FIXED) | Resolved | `model Agent` in `api/prisma/schema.prisma` now carries `organizationId`, `projectId`, `teamId` (with indexes). |
| R5 | #11 "Public channels require membership to be visible" (NOT FIXED) | Resolved | `api/src/services/channels.ts:39` includes `{ visibility: 'public' }` in listing and `:295-300` grants public-channel access without a membership row. |
| R6 | #2 "Shared-agent REST endpoints leak cross-channel history" (NOT FIXED) | Resolved | `api/src/services/agent-read-model.ts:360-385` scopes agent reads by channel scope through `buildAccessibleChannelWhere(visibility)`; owner-only widening is gated by `createAgentVisibilityScope` (`api/src/lib/request-helpers.ts:440`). |
| R7 | #6 "Login and SSO auto-provision hardcode bootstrap org/project/team IDs" (NOT FIXED) | Resolved (see L4) | `api/src/routes/auth-core.ts` + `api/src/services/auth.ts` resolve real memberships (`loadUserMemberships`) and error `NO_MEMBERSHIP` instead of issuing reserved bootstrap IDs. The residual single-active-membership limit is tracked as L4. |
| R8 | #15 "Admin WS client missing ping; SSE missing `Last-Event-ID` reconnect" (NOT FIXED) | Resolved | SSE clients send `Last-Event-ID` on reconnect (`admin/src/facades/threads/hooks.ts:92-93`, `admin/src/facades/notifications/useMessageNotifications.ts:363-364`); the server keeps a replay store (`api/src/realtime/hub.ts`), and `PresenceProvider` runs a keepalive heartbeat (`admin/src/providers/PresenceProvider.tsx:94`). |

---

*Not re-verified this pass (carried from `docs/implementation-phases.md` without
independent code confirmation): #13 channel-listing N+1 and #14 activity-query
scaling. They are omitted from the tables above rather than asserted, to keep
this register limited to checked claims. Verify against
`api/src/services/channels.ts` and the agent activity loaders before relying on
their status.*

---

*Tenant isolation — the invariant that a caller in org B cannot read or mutate
org A's rows — is machine-checked by the conformance suite in
`api/test/conformance/`. See [tenant-isolation-testing.md](tenant-isolation-testing.md)
for the resource×check matrix, how to add a row when introducing a route, and
what it deliberately does not prove (no live-DB boot, no mutation-testing of
scope clauses yet).*
