// The suite's assertion vocabulary, re-exported under this directory so a case
// never has to know where the navigation suite keeps it. One import path for
// every case, whichever phase wrote it.
export { CaseFailure, createChecks } from '../../navigation/lib/expect.mjs'
