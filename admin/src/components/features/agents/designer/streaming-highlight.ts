/**
 * The border + ring the Design Assistant paints on a field while it is live
 * writing it — a real, useful affordance unique to this designer. It was five
 * independently hand-typed copies of the same class string; this is the one
 * name for it.
 */
export const STREAMING_HIGHLIGHT_CLASS = 'border-[var(--accent)] shadow-[0_0_0_1px_var(--accent-soft)]'
