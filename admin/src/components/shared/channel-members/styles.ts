/** Shared layout class strings for channel-members popup rows. */
export const rowClass = [
  'flex items-center gap-3 rounded-lg px-3 py-2',
  'hover:bg-white/5 transition-colors',
].join(' ')

/** Shared class string for inline row action buttons. */
export const actionBtnClass = [
  'flex h-7 items-center justify-center rounded px-2',
  'text-xs font-medium transition-colors',
].join(' ')

/** Section heading class shared by both list sections. */
export const sectionHeadingClass = [
  'px-3 py-1.5 text-[11px] font-semibold uppercase',
  'tracking-[0.16em] text-[color:var(--tx3)]',
].join(' ')
