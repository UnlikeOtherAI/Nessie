# This package is vendored byte-for-byte from its pinned UOA revision. Prevent
# core.autocrlf from altering its upstream LF bytes, so its generated-artifact
# and SHA-256 gates behave identically on every development platform.
packages/billing-statement-protocol/** -text
