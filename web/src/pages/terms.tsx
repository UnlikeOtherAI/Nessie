// Placeholder. The page's prose is written into this file; the title, the
// lede, the route and every link to it come from `registry.ts`.
export const TermsPage = () => (
  <p className="n-placeholder">This page is being written.</p>
)
