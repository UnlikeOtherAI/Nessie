/** API compatibility export; policy is shared with the standalone worker. */
export {
  DOMAIN_CLASSIFIER_VERSION,
  DomainPolicyError,
  assertAutomaticMembershipDomainAllowed,
  automaticMembershipTxtName,
  normalizeAutomaticMembershipDomain,
} from '@nessie/team-admin'
